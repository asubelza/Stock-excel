version: '3'

services:

  stock:
    build: .
    container_name: stock-excel_stock_1
    ports:
      - "8001:5000"
    environment:
      - DATABASE_URL=postgresql://stockuser:stockpass123@stock_db:5432/stockdb
    depends_on:
      - stock_db
    networks:
      - web_ecjy_app-network
    restart: unless-stopped

  stock_db:
    image: postgres:15-alpine
    container_name: stock-excel_db_1
    environment:
      POSTGRES_USER: stockuser
      POSTGRES_PASSWORD: stockpass123
      POSTGRES_DB: stockdb
    volumes:
      - stock_db_data:/var/lib/postgresql/data
    networks:
      - web_ecjy_app-network
    restart: unless-stopped

networks:
  web_ecjy_app-network:
    external: true

volumes:
  stock_db_data: